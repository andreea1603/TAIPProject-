CREATE TABLE testResult (ID LONGVARBINARY NOT NULL, TESTDATE TIMESTAMP, TESTRESULT INTEGER, USERID LONGVARBINARY, PRIMARY KEY (ID))
CREATE TABLE users (ID LONGVARBINARY NOT NULL, BIRTHDATE TIMESTAMP, CITY VARCHAR, COUNTRY VARCHAR, EMAILADDRESS VARCHAR, FIRSTNAME VARCHAR, GENDER BOOLEAN, HANDEDNESS BOOLEAN, LASTNAME VARCHAR, PASSWORDHASH VARCHAR, PHONENUMBER VARCHAR, PROVINCE VARCHAR, USERNAME VARCHAR, PRIMARY KEY (ID))
CREATE TABLE mri (ID LONGVARBINARY NOT NULL, IMAGE LONGVARBINARY, USERID LONGVARBINARY, PRIMARY KEY (ID))
CREATE TABLE question (ID LONGVARBINARY NOT NULL, IMAGE LONGVARBINARY, OPTION1 VARCHAR, OPTION2 VARCHAR, QUESTION VARCHAR, RESPONSE VARCHAR, PRIMARY KEY (ID))
ALTER TABLE users ADD CONSTRAINT UNQ_users_0 UNIQUE (emailAddress, username)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT NUMERIC(38), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
